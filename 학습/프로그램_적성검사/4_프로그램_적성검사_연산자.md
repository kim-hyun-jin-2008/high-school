# 3. 연산자

아래를 보기전에 시청 영상: https://www.youtube.com/watch?v=_h1N7vH1waM&list=PLFzP8RKjiyFEVJAtARaUsRMPM0u-rd0kF&index=25

## 📘 1. 사칙연산(+, -, ×, ÷)

### ✔ 무엇인지?

숫자끼리 **더하고, 빼고, 곱하고, 나누는** 연산이야.
수학 시간에 하는 계산이랑 똑같아!

| 기호  | 이름  | 설명             |
| --- | --- | -------------- |
| `+` | 더하기 | 두 숫자를 합함       |
| `-` | 빼기  | 앞 숫자에서 뒤 숫자를 뺌 |
| `*` | 곱하기 | 앞 숫자와 뒤 숫자를 곱함 |
| `/` | 나누기 | 앞 숫자를 뒤 숫자로 나눔 |

---

### ✔ 실습 예제 (Node.js)

```js
console.log("=== 사칙연산 ===");
console.log("3 + 2 =", 3 + 2);
console.log("5 - 1 =", 5 - 1);
console.log("4 * 3 =", 4 * 3);
console.log("10 / 2 =", 10 / 2);
```

### 👉 출력 예

```
=== 사칙연산 ===
3 + 2 = 5
5 - 1 = 4
4 * 3 = 12
10 / 2 = 5
```

---

## 📘 2. 비교연산 (큰지, 작은지, 같은지 비교하기)

### ✔ 무엇인지?

두 값을 **비교해서 맞으면 true(참)**,
틀리면 **false(거짓)** 을 알려주는 연산이야.

| 기호    | 뜻      | 설명           |
| ----- | ------ | ------------ |
| `>`   | 크다     | 왼쪽이 더 큰가?    |
| `<`   | 작다     | 왼쪽이 더 작은가?   |
| `>=`  | 크거나 같다 | 왼쪽이 크거나 같은가? |
| `<=`  | 작거나 같다 | 왼쪽이 작거나 같은가? |
| `===` | 같은가?   | 완전히 같은 값인가?  |
| `!==` | 다른가?   | 값이 다른가?      |

---

### ✔ 실습 예제 (Node.js)

```js
console.log("=== 비교연산 ===");
console.log("3 > 2 =", 3 > 2);
console.log("5 < 1 =", 5 < 1);
console.log("3 === 3 =", 3 === 3);
console.log("3 !== 2 =", 3 !== 2);
```

### 👉 출력 예

```
=== 비교연산 ===
3 > 2 = true
5 < 1 = false
3 === 3 = true
3 !== 2 = true
```

---

## 📘 3. 논리연산 (그리고 / 또는 / 아니야)

### ✔ 무엇인지?

논리연산은 **참(true)/거짓(false)** 을 가지고 판단하는 연산이야.

| 기호     | 이름       | 쉽게 설명          |
| ------ | -------- | -------------- |
| `&&`   | 그리고(AND) | 둘 다 참이어야 참     |
| `\|\|` | 또는(OR)   | 둘 중 하나만 참이면 참  |
| `!`    | 아니다(NOT) | 참이면 거짓, 거짓이면 참 |

🔍 쉽고 귀여운 비유:

* AND: **엄마가 "청소도 하고 숙제도 하면 TV 봐!"**
* OR: **엄마가 "청소하거나 숙제 둘 중 하나만 하면 TV 봐!"**
* NOT: “TV 보면 안 돼!” → **못 보면 OK, 보면 안됨**

---

### ✔ 실습 예제 (Node.js)

```js
console.log("=== 논리연산 ===");
console.log("true && false =", true && false);
console.log("true || false =", true || false);
console.log("!true =", !true);
console.log("!false =", !false);
```

### 👉 출력 예

```
=== 논리연산 ===
true && false = false
true || false = true
!true = false
!false = true
```

---

## 📘 전체 요약 표

| 종류   | 쉽게 설명          | 예시            |
| ---- | -------------- | ------------- |
| 사칙연산 | 숫자 계산          | 3 + 2         |
| 비교연산 | 값을 비교해 참/거짓 판단 | 3 > 1         |
| 논리연산 | 참·거짓을 합쳐 판단    | true && false |

---


## 🌳 1. 연산자 마인드맵 스타일

```
                   [연산자 Operators]
                             │
 ┌───────────────────────────┼──────────────────────────────┐
 │                           │                              │
 ▼                           ▼                              ▼
[사칙연산]               [비교연산]                     [논리연산]
 +  더하기                >   크다                      &&  그리고(AND)
 -  빼기                  <   작다                     ||  또는(OR)
 *  곱하기                >=  크거나 같다               !   아니다(NOT)
 /  나누기                <=  작거나 같다
                          == 같다
                          != 다르다
                          === 완전히 같다
                          !== 다르다
```

---

## 📘 2. 중학생/고등학생 버전 심화 설명


### ✔ 사칙연산 (Arithmetic Operators)

**숫자 데이터(Number Type)** 를 계산할 때 사용한다.

| 연산자 | 의미  | 비고                       |
| --- | --- | ------------------------ |
| `+` | 더하기 | 문자열과 함께 쓰이면 “문자열 붙이기”가 됨 |
| `-` | 빼기  | 숫자만 계산                   |
| `*` | 곱하기 | 숫자 계산                    |
| `/` | 나누기 | 0으로 나누면 `Infinity`       |
| `%` | 나머지 | 배수/짝수/홀수 판별에 자주 사용       |

### 예

```js
console.log(10 % 2); // 0 → 짝수
console.log(10 % 3); // 1
```

---

### ✔ 비교연산 (Comparison Operators)

두 값의 **크기 또는 동일함을 판단**해서 `true` 또는 `false`를 만들어준다.

| 연산자   | 설명             |
| ----- | -------------- |
| `>`   | 왼쪽이 큰가?        |
| `<`   | 왼쪽이 작은가?       |
| `>=`  | 큰가 또는 같은가?     |
| `<=`  | 작은가 또는 같은가?    |
| `==` |  같은가? |
| `!=` |  다른가? |
| `===` | 자료형까지 완전히 같은가? |
| `!==` | 자료형까지 완전히 다른가? |

### 중요한 점

`===` 는 **자료형(Type)까지 비교**한다.

```js
console.log(3 === "3"); // false
```

---

### ✔ 논리연산 (Logical Operators)

여러 개의 조건을 조합하거나 반대로 뒤집을 때 사용한다.

| 연산자  | 설명             |   |                |
| ---- | -------------- | - | -------------- |
| `&&` | 둘 다 참일 때만 참    |   |                |
| `\|\|`     | 둘 중 하나라도 참이면 참 |
| `!`  | 참이면 거짓, 거짓이면 참 |   |                |



### 예

```js
let age = 15;
console.log(age >= 14 && age <= 19); // true (청소년)

let age = 15;
console.log(age >= 17 && age <= 19); // false

let age = 15;
console.log(age >= 17 || age <= 19); // true
console.log(age >= 17 || age <= 11); // false

let age = 15;
console.log(age == 14 ); //
console.log(age != 14 ); //
console.log(age == 15 ); //
console.log(age != 15 ); //
```

---

## 📝 3. 실습 문제(퀴즈)

### ■ 문제 1 — 사칙연산

다음 코드의 출력 결과는?

```js
console.log(8 + 5 * 2);
```

정답:

```
18
```

왜?
곱하기 먼저 → 5*2 = 10 → 8+10 = 18

---

### ■ 문제 2 — 비교연산

아래의 결과는 무엇일까?

```js
console.log(10 > 9);
console.log(10 === "10");
console.log(10 !== 9);
```

정답:

```
true
false
true
```

---

### ■ 문제 3 — 논리연산

```js
console.log(true && false);
console.log(true || false);
console.log(!false);
console.log(3 > 1 && 2 > 5);
```

정답:

```
false
true
true
false
```

---

### ■ 문제 4 — 종합 실습

학생 점수가 다음과 같을 때, 합격(true)/불합격(false)을 판단해보자.

조건:

* 평균이 60점 이상 AND
* 결석이 5회 이하

```js
let math = 70;
let english = 50;
let absence = 3;

let avg = (math + english) / 2;

console.log(avg >= 60 && absence <= 5);
```

정답:

```
true
```

---

## ❗ 4. 잘 틀리는 개념 정리



### ✔ 1) `===` vs `==`

* `===` → 자료형까지 완전히 비교 (권장!)
* `==` → 자료형 다르면 변환해서 느슨하게 비교함 (헷갈림 주의)

```js
3 == "3";   // true
3 === "3";  // false
```

---

### ✔ 2) + 연산자는 숫자 더하기 또는 문자열 연결

```js
5 + 5;        // 10
"5" + "5";    // "55"
5 + "5";      // "55"
```

숫자 + 문자열 = 문자열 붙이기 → 많이 틀림!

---

### ✔ 3) 비교연산의 결과는 항상 true/false

```js
console.log(3 > 1); // true
console.log(3 < 1); // false
```

---

### ✔ 4) 논리연산은 조건끼리 연결해서 판단

`true && false = false`
`true || false = true`

---

### ✔ 5) 0으로 나누면 에러가 아니라 Infinity
0으로 나누는 것은 수학적으로 잘못된 행동
* 그런데 JavaScript는 왜 오류 대신 Infinity를 줄까?
* 그 이유는 JavaScript가 'IEEE 754 부동소수점 규칙' 을 따르기 때문이다.

#### ✔ 간단한 비유

컵에 물을 “0컵으로 나누어 나눠 가지기”는 불가능하다.
그래서 계산이 끝도 없이 커져버리는 것처럼
무한대(Infinity) 라고 컴퓨터가 표시해주는 것.

```js
console.log(10 / 0);   // Infinity
console.log(-10 / 0);  // -Infinity
console.log(1 / 0);    // Infinity
console.log(0 / 0);    // NaN (숫자가 아님)

```


---

## 🧪 🔥 난이도 상승 문제 세트 (중급 → 고급)

---

### ■ 문제 1 — 계산 순서 제대로 이해하기 (중급)

다음 코드의 출력값은?

```js
console.log(10 - 3 * 2 + 8 / 4);
```

#### 힌트

*곱하기·나누기 먼저, 그다음 더하기·빼기*

---

### ■ 문제 2 — 비교연산 혼합 (중급)

다음의 결과는?

```js
console.log(5 * 2 === 10);
console.log(5 * 2 > 10);
console.log(20 / 4 <= 5);
```

---

### ■ 문제 3 — 문자열 + 숫자 주의 (중급~상급)

결과가 어떻게 될까?

```js
console.log("10" + 2);
console.log(10 + "2");
console.log("10" - 2);
console.log("10" * 2);
```

---

### ■ 문제 4 — 논리연산자 우선순위 (상급)

다음 출력은?

```js
console.log(true || false && false);
console.log((true || false) && false);
```

#### 힌트

* `&&` 가 `||` 보다 먼저 계산됨
* 괄호가 있으면 괄호 먼저

---

### ■ 문제 5 — 조건 판별 심화 (상급)

점수가 아래와 같을 때 나타나는 결과는?

조건

* 평균 ≥ 70
* 3과목 중 1과목이라도 40점 미만이면 무조건 불합격

```js
let kor = 80;
let eng = 35;
let math = 90;

let avg = (kor + eng + math) / 3;

let isPass = avg >= 70 && kor >= 40 && eng >= 40 && math >= 40;

console.log(isPass);
```

---

### ■ 문제 6 — 논리연산 + 비교연산 종합 (상급)

```js
let a = 5;
let b = 10;
let c = 15;

console.log(a < b && b < c);
console.log(a > b || b < c);
console.log(!(a + b > c));
```

---

### ■ 문제 7 — 자료형 혼합 비교 (상급)

```js
console.log(10 == "10");
console.log(10 === "10");
console.log("5" * 2 === 10);
console.log("5" + 2 === 52);
```

---

### ■ 문제 8 — 조건 2단계 판정 문제 (고급)

조건

* 나이가 14~19 사이면 “청소년”
* 20~64면 “성인”
* 65 이상은 “노인”

각 연산자 사용해서 판정하라.

```js
let age = 67;

let isTeen = age >= 14 && age <= 19;
let isAdult = age >= 20 && age <= 64;
let isSenior = age >= 65;

console.log(isTeen, isAdult, isSenior);
```

---

### ■ 문제 9 — 논리연산자로 복잡한 조건 만들기 (고급)

아래 조건을 논리연산자로 표현하기:

조건

* 키 140cm 이상
* 나이 10세 이상
* 둘 중 하나라도 만족하면 놀이기구 탑승 가능
* 단, **심장 질환자는 무조건 불가**

```js
let height = 130;
let age = 12;
let heart = false; // false = 질환 없음

let canRide = (height >= 140 || age >= 10) && !heart;

console.log(canRide);
```

---

### ■ 문제 10 — 정말 어려운 최상위 난이도 (고급)

아래 규칙을 모두 만족하는지 출력하라.

조건

* 수학 점수 > 영어 점수
* 두 점수의 차이가 10 이상
* 두 점수의 합이 150 이상
* 평균이 80 이상
* 둘 중 하나라도 40 미만이면 불합격

```js
let math = 95;
let eng = 70;

let diff = math - eng;
let sum = math + eng;
let avg = sum / 2;

let result =
  math > eng &&
  diff >= 10 &&
  sum >= 150 &&
  avg >= 80 &&
  math >= 40 &&
  eng >= 40;

console.log(result);
```


